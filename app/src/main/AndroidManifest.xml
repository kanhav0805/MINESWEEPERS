<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minesweepers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MINESWEEPERS">
        <activity android:name=".MineColor"></activity>
        <activity
            android:name=".SetMineColor"
            android:label="@string/title_activity_set_mine_color"
            android:theme="@style/Theme.MINESWEEPERS.NoActionBar" />
        <activity android:name=".WinningScreen" />
        <activity android:name=".GameLost" />
        <activity android:name=".Gaming" />
        <activity android:name=".CustomBoardActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>